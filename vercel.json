{
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "export appodus_active_env=dev && alembic upgrade head && gunicorn -w 4 -k uvicorn.workers.UvicornWorker  appodus:fast_api_app",
  "builds": [
    {"src": "/appodus.py", "use": "@vercel/python"}
  ],
  "routes": [
    {"src": "/(.*)", "dest": "appodus.py"}
  ]
}
